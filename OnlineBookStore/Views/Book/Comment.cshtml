@model OnlineBookStore.Models.Entities.BOOK

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Comment</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                Tên người dùng
            </th>
            <th>
                Ngày đánh giá
            </th>
            <th>
                Nội dung
            </th>
            <th>
                Đánh giá
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model.COMMENTDTO)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.USERNAME)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.COMMENTDATE)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.COMMENT)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RATING)
                </td>
                <td>
                    @Html.ActionLink("Delete", "DeleteComment", new { id = item.COMMENTID })
                </td>
            </tr>
        }

    </table>
    <br />

    @*<div class="text-center" style="text-align: center">
            <ul class="pagination">
                Trang  @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)
                / @Model.PageCount

                @Html.PagedListPager(Model, pageNum => Url.Action("Comment", new
                {
                    pageNum
                }))
            </ul>
        </div>*@

</body>
</html>
